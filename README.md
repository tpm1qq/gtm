# gtm - global theme manager

gtm is a small CLI tool that allows for configuration of various different packages via a single command or config file.

currently only supports adjusting color settings with a single command per package; config functionality to be implemented.

